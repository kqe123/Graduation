<!-- edit_text_box_bg.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/system_accent1_10" /> <!-- 배경색을 흰색으로 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/black"/> <!-- 테두리 색상 -->
    <corners android:radius="20dp" /> <!-- 상자 모서리의 반경 설정 -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="4dp"
        android:bottom="4dp"/>
</shape>